rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Temporarily open for the activity
      allow read, write: if true;
    }
  }
}
